#include<stdio.h>

/**
 * main - Entry point
 *
 * Description: printing the base 16 in lowercase
 *
 * Return: 0
*/

int main(void)
{
	int digit = 48;

	while (X <= 102)
	{
		putchar(X);
		if (X == 57)
			X += 39;
		++X;
	}
	putchar('\n');

	return (0);
}
