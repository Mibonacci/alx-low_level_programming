#include "main.h"
/**
 * more_numbers - print 10 times the numbers
 *
 */

void more_numbers(void)
{
	int  x, y;

	for (y = 0; y <= 9; y++)
	{
		for (x = 48; x <= 49 52 ; x++)
		_putchar(x);
	_putchar('\n');
	}
}
