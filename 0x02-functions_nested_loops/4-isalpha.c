#include "main.h"

/**
 * _isalpha - function
 *
 * @c: the parameter to check with
 *
 * Return: comes with 1 if a letter. otherwise 0
 */

int _isalpha(int c);

	if (c >= 65 && c <= 122)
		return (1);
	else
		return (0);
